debug: true
server:
  port: '8083 '
spring:
  application:
    name: JwtDemo
  datasource:
    password: shivam
    url: jdbc:mysql://localhost:3306/jwtdb
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
